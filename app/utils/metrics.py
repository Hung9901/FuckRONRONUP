# Integrate Prometheus or OpenTelemetry for monitoring
# Example with prometheus_client:
#
# from prometheus_client import Counter, Histogram
#
# messages_received = Counter("messages_received_total", "Total WebSocket messages received")
# processing_latency = Histogram("processing_latency_seconds", "Task processing latency")
